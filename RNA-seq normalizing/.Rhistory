q()
library(synapseClient)
source('http://depot.sagebase.org/CRAN.R')
update.package(c("synapseClient"))
pkgInstall(c("synapseClient"))
install.packages(package, repos = c(sageRepo, "http://cran.r-project.org"),
)
library(synapseClient)
session
sessionInfo
sessionInfo()
library(synapseClient)
library(R.utils)
library(edgeR)
exit()
quit()
sessionInfo()
library(synapseClient)
library(R.utils)
library(edgeR)
tauZipfile <- synGet('syn2875347')
localFilePath <- getFileLocation(tauZipfile)
synapseLogin()
packageStatus()
install.packages(c("manipulate", "Rcpp"))
packageStatus()
remove.package("synapseClient")
?pkgRemove
?pkgInstall
?ls
?pkg
??pkg
pkgInstall("synapseClient")
exit()
quit()
q()
require("synapseClient")
require("R.utils")
require("edgeR")
synapseLogin()
mergedCountFile <- ('3160706')
originalCountFile <- synGet(mergedCountFile)
mergedCountFile <- ('syn3160706')
originalCountFile <- synGet(mergedCountFile)
localFilePath <- getFileLocation(originalCountFile)
if(!file.exists(substr(localFilePath, 1, nchar(localFilePath) - 3))) {
gunzip(localFilePath)
}
localFilePath <- sub('.gz', '', localFilePath) #trim the .gz suffix
rawCounts <- read.table(localFilePath, header = TRUE)
setwd("~/Projects/UO1-AMP/working/RNA-seq normalizing")
transposedCounts <- t(rawCounts)
str(transposedCounts)
min(transposedCounts)
transposedCounts[(apply(transposedCounts, 1, function(y) any(y == 0))),]
dim(transposedCounts[(apply(transposedCounts, 1, function(y) any(y == 0))),])
?sapply
?apply
?lapply
apply(transposedCounts,1, FUN=function(y){sum(y == 0)})
dim(apply(transposedCounts,1, FUN=function(y){sum(y == 0)}))
hist(transposedCounts)
?which
which(transposedCounts == 0)
sum(which(transposedCounts == 0))
sum(as.numeric(which(transposedCounts == 0)))
hist(transposedCounts)$counts
df[rowSums(df[, -1])>0, ]
transposedCounts[rowSums(transposedCounts[, -1])>0, ]
dim(transposedCounts[rowSums(transposedCounts[, -1])>0, ])
dim(transposedCounts)
39179-28927
str(rawCounts)
dim(rowSums(transposedCounts < 1) >= 5)
dim(rowSums(transposedCounts > 1) >= 5)
rowSums(transposedCounts > 1) >= 5
sum(as.number(rowSums(transposedCounts > 1) >= 5)
)
?as
sum(as.numeric(rowSums(transposedCounts > 1) >= 5))
sum(as.numeric(rowSums(transposedCounts < 1) >= 5))
sum(as.numeric(rowSums(transposedCounts < 1) >= 55))
sum(as.numeric(rowSums(transposedCounts < 1) >= 60))
sum(as.numeric(rowSums(transposedCounts < 1) >= 61))
sum(as.numeric(rowSums(transposedCounts < 1) >= 59))
